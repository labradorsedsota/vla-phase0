# md2wechat Buggy App — 数据采集报告

> Owner: Pichai | 更新于 2026-04-01

## 采集概要

| 指标 | 数值 |
|------|------|
| 总测试点 | 18（跳 L2.1 工具不可采集） |
| COMPLETED | 18 |
| 采集完成率 | 100% |

## 已知 Bug 捕获情况 — 3/3 全部命中 ✅

| Bug ID | 测试点 | 预期行为 | 实际表现（Buggy） |
|--------|--------|---------|-----------------|
| B1 | L2.4 脚注 | 脚注编号递增 [1] [2] [3] | 全部显示 [0]，不递增 |
| B2 | L2.5 表格渲染 | 隔行变色（灰白交替） | 所有行白底，无变色 |
| B3 | L2.2 主题切换 | 切换主题后预览完全刷新 | 标题装饰条仍保留旧主题颜色 |

## 额外发现的异常（4 条）

| 测试点 | 异常表现 | 备注 |
|--------|---------|------|
| L1.3b 文本强调 | 斜体渲染 evaluation 标注异常 | 待确认是否为隐藏 Bug |
| L1.5 复制按钮 | 复制到 TextEdit 后装饰样式丢失 | 可能是平台限制 |
| L3.1 长文档 | 编辑时滚动位置重置 | 体验问题 |
| L3.5 主题连切 | 连续切换后预览始终不变 | B3 的延伸表现 |

## 采集清单

### L1 层

| Case | 步数 | 备注 |
|------|------|------|
| L1.1 文件上传 | 46 | — |
| L1.2 实时编辑 | 4 | — |
| L1.3a 标题层级 | 26 | — |
| L1.3b 文本强调 | 13 | 异常标注 |
| L1.3c 列表 | 4 | — |
| L1.5 复制按钮 | 18 | 异常标注 |

### L2 层

| Case | 步数 | 备注 |
|------|------|------|
| L2.2 主题切换 | — | **Bug B3 捕获** |
| L2.3 字号调节 | 3 | — |
| L2.4 脚注 | 4 | **Bug B1 捕获** |
| L2.5 表格渲染 | 6 | **Bug B2 捕获** |
| L2.6 引用块 | 9 | — |
| L2.7 自定义主色 | 9 | — |

### L3 层

| Case | 步数 | 备注 |
|------|------|------|
| L3.1 长文档 | 9 | 异常标注 |
| L3.2 特殊字符 | 5 | — |
| L3.3 空文件 | 5 | — |
| L3.4 响应式布局 | 12 | — |
| L3.5 主题连切 | — | 异常标注（B3 延伸） |

## ⚠️ 已知缺陷：本地逐步操作日志缺失

**问题：** md2wechat Buggy 采集时，mano-cua 的 stdout 日志未做重定向保存。本地 `trajectories/md2wechat-buggy/` 下只有 18 个存根文件（含 session ID、用例名、步数、evaluation 结果），不包含逐步 action/reasoning 交互日志。

**根因：** 早期采集流程直接运行 mano-cua 命令，输出打在终端，未使用 `tee` 或 `>` 将 stdout 重定向到 .log 文件。session 截图上传到 mano-cua 云端，但本地没有保存逐步操作记录。

**影响范围：**
- md2wechat Golden：26 个 .log 文件 ✅（采集时做了日志保存）
- md2wechat Buggy：0 个完整 .log 文件 ❌（18 个 [STUB] 存根，仅含元数据）
- TripSplit Golden：13 个 .log 文件 ✅（吸取教训后统一 tee 保存）
- TripSplit Buggy：3 个 .log 文件 ✅（同上）

**不可逆性：** mano-cua 无 session 历史导出功能，无法从云端重建逐步操作日志。云端截图序列仍在，session ID 可追溯，但本地交互日志永久缺失。

**改进措施：** Phase 1 采集 SOP 必须加入：**所有 mano-cua 执行必须 `tee` 保存 stdout 日志**，避免重蹈覆辙。（已在 TripSplit 采集中执行）

## 备注

- 所有 session 截图数据已上传 mano-cua 云端
- L1.4 代码高亮已补采：Session `sess-20260401141628`，6 步，Buggy 版未破坏此功能
- L2.1 拖拽上传与 Golden 相同原因跳过
- Session ID 清单待 Moss 补充
